input {
  tcp {
    port => 5001
    codec => json
  }
  # You can also add a beats input if using Filebeat:
  # beats {
  #   port => 5044
  # }
}

filter {
  # Add your filter logic here, e.g. grok, date, etc.
  # This section is highly customizable based on your needs.
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    user => "logstash_user"
    password => "Iphonesucks@94"
    index => "logstash-%{+YYYY.MM.dd}"
    # Optional: Manage document IDs, retries, etc.
  }
  stdout { codec => rubydebug }
}
